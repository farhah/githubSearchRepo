import mongoose from 'mongoose'
import searchService from './search.services'

describe("Save search queries", () => {

    it("Should save search query and results made by a user", () => {
        // searchService.saveSearchQuery(userId, query, language, topic, url, page, itemsPerPage, result);
    })
})